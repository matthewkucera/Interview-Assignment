<%= form_for(@pencil) do |f| %>
  <% if @pencil.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pencil.errors.count, "error") %> prohibited this pencil from being saved:</h2>

      <ul>
      <% @pencil.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
